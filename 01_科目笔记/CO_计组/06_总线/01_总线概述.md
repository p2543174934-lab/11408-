## 总线的定义
- 总线是一组能为多个部件分时共享的公共信息传送线路
## 总线的特性
- 机械特性：尺寸、形状、管脚数、排列顺序
- 电气特性：传输方向和有效电平范围
- 功能特性：每根传输线的功能
- 时间特性：信号的时序关系
## 总线的分类
### 按数据传输格式
- 串行总线：每次传送1bit
	- 优点：成本低，广泛应用于长距离传输，节省空间
	- 缺点：数据发送和接收时要进行拆卸和装配，考虑串行-并行转换的问题 
- 并行总线：多bit
	- 优点：逻辑时序简单，电路实现起来比较容易
	- 缺点：信号线数量多，占用更多的布线空间；远距离传输成本高昂
### 按总线功能
- 片内总线
	- CPU芯片内部寄存器与寄存器之间、寄存器与ALU之间的公共连接线
- 系统总线
	- 计算系统内部个功能部件之间相互连接的总线
	- 数据总线：位数与机器字长、存储字长有关；双向
	- 地址总线：传输地址信息；位数与主存地址空间大小以及设备数量有关；单向
	- 控制总线：一根传输一个信号；对于单独一根单向。但是有出有入：CPU发出，其他设备向CPU反馈
- 通信总线
	- 通信总线是用于计算机系统之间与其他系统之间信息传送的总线，通信总线也称为外部总线
### 时序控制方式
- 同步总线
- 异步总线

## 系统总线的结构
- 单总线结构
	- 优点：结构简单、成本低、易于接入新设备
	- 缺点：带宽低，负载重，多个部件只能争用唯一的总线，不支持并行
- 双总线结构
	- 一条主存总线，用于CPU、主存和通道之间进行数据传送（支持突发传送：送出一个地址、收到多个地址连续的数据）
	- 一条I/O总线，用于多个外部设备与通道之间进行数据传送
	- 优点：将较低速I/O设备从单总线分离
	- 缺点：要增加通道等硬件设备
- 三总线结构
	- 三条总线分别是主存总线，I/O总线，和直接内存访问DMA总线![[Pasted image 20260611161347.png]]
	- 优点：提高了I/O设备的性能，更快响应命令，提高系统吞吐量
	- 缺点：系统的工作效率较低（三个总线同时只能有一个工作）
- 四总线结构
	- ![[Pasted image 20260611161548.png]]
	- 四条总线速度不同
	- 桥接器：用于连接不同的总线，具有数据缓冲，转换和控制的功能
	- 靠近CPU总线速度较快
	- 每级总线设计遵循总线标准