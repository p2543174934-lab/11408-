- 与分页最大区别就是 -- 离散分配时所分配地址空间的基本单位不同

## 分段

- 进程的地址空间：按照==程序自身逻辑关系划分==若干个段，每个段都有一个段名，每段从0开始编址
- 内存分配规则：以段为单位进行分配，每个段在内存中占据连续空间，但各段之间可以不相邻
- 因为按逻辑功能划分，所以用户编程更方便，程序可读性更高
- 分段系统的逻辑地址结构由段号+段内地址所组成
	- 段号位数决定了每个进程最多分几个段
	- 段内地址位数决定了每个段的最大长度是多少


## 段表

- 为每个进程建立一张段映射表，简称段表
- ![[Pasted image 20260628202821.png]]
- 每个段对应一个段表项，其中记录了该段在内存中的起始位置，和段的长度
- 各个段表项的长度是相同的，因此段号可以使隐含的，不占存储空间

## 地址变换

- ![[Pasted image 20260628203457.png]]
- 在分页时，每个页面长度是相同的，但是分段时不同，所以必须检查==段内地址是否越界==
- 两次访存
	- 第一次：查内存中的段表
	- 第二次：访问目标内存单元
- 分段系统也可以引入快表

## 分段、分页管理的对比

- 页是信息的物理单位，分页的主要目的是为了实现离散分配，提高内存利用率。分页仅仅是系统管理上的需要，完全是系统行为，==对用户是不可见的==
- 段是信息的逻辑单位，分段的主要目的是更好的满足用户需求。一个段通常包含着一组属于一个逻辑模块的信息，分段是==对用户可见==的，用户编程时需要显式的给出段名
- 页的大小固定且由系统决定，段的长度不固定，决定于用户编写的程序
- 分页的用户进程地址空间是==一维==的，程序员只需要给出一个记忆符即可表示一个地址
- 分段的用户进程地址空间是==二维==的。程序员既要给出段名，也要给出段内地址


## 段的共享与保护

- **分段比分页更容易实现信息的共享和保护**
- 不能被修改的代码称为纯代码或可重入代码，这样的代码是可以共享的
- 只需要在每个进程的段表中设置一个段表项指向这个共享段，实现共享极为便捷
- 可维护一张共享段表，短号，段长，内存起始地址，存在位，外存起始地址，共享进程计数count
- 分段系统的保护机制
	- 地址越界保护 -- 段号≥段表长度，越界；段内偏移≥段长，越界；
	- 存取控制保护 -- 访问权限位防止非法访问