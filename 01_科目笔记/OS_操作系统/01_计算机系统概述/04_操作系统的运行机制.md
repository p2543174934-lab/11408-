内核时操作系统最重要最核心的部分，也是最接近硬件的部分
CPU在设计和生产的时候就划分了特权指令和非特权指令
## 两种指令
- 特权指令：只允许管理者即操作系统内核来使用
- 非特权指令
## 两种处理机状态
- 内核态（管态）：处于内核态，正在运行内核程序，允许执行特权指令
- 用户态（目态）
- 内核态和用户态的切换
	- 内核态 -> 用户态：内核会在让出CPU之前，用一条特权指令把PSW的标志位设置为用户态
	- 用户态 -> 内核态：由中断引发，硬件自动完成变态过程
## 两种程序
- 内核程序
- 应用程序

