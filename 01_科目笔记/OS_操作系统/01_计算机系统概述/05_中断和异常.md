## 中断的作用

- 中断是让操作系统内核夺回CPU使用权的唯一途径，用户态->内核态
## 中断的类型

异常和中断！！！
- 内中断：与当前执行的指令有关，中断信号来源于CPU内部
	- 用户态运行特权指令
	- 除数为0
	- 陷入指令（应用程序想要请求操作系统内核的服务）
- 外中断：与当前执行的指令无关，中断信号来源于CPU外部；每条指令执行结束会检查是否有外中断
	- 时钟中断（时钟部件发来的中断信号）
	- IO中断

## 中断的机制

- 不同的中断信号需要用不同的中断程序来处理，会根据中断信号的类型查询中断向量表

