## 磁盘初始化

- 进行低级格式化（物理格式化），将磁盘各个磁道划分为扇区
- 将磁盘分区，每个分区由若干柱面组成
- 进行逻辑格式化，创建文件系统（文件系统的根目录，初始化存储空间管理的数据结构）


## 引导块

- 完整的自举程序放在磁盘的启动块（引导块/启动分区）上，启动快位于磁盘的固定位置
- 拥有启动分区的磁盘称为启动磁盘或系统磁盘

## 坏块的管理

- 对于简单的磁盘可以在逻辑格式化时对整个磁盘进行检查，在FAT表上标明哪些时坏块
- 复杂磁盘磁盘控制器会维护一个坏块链表，在物理格式化就对坏块链进行初始化
	- 会保留一些备用扇区用来替换坏块，坏块对操作系统透明
	