- 滑动窗口机制
	- 发送窗口 > 1
	- 接收窗口 = 1
- 确认机制
	- 确认帧ACK_i
- 重传机制
	- 超时重传
- 帧编号
	- 至少nbit
- 特殊规则
	- 接收方可以累计确认，即连续收到多个数据帧时，可以仅返回最后一个帧的ACK
	- 若发送方超时未收到ACK_i，重传i以及之后的帧

- 异常情况
	- 数据帧丢失
		- 超时，重传i及其后续的所有帧  -- 流量控制
	- 确认帧丢失
		- 超时重传，返回最后一个ACK
![[Pasted image 20260707150418.png]]