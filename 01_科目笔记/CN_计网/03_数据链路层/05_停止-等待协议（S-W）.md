- 滑动窗口机制
	- 发送窗口 = 1
	- 接受窗口 = 1
- 确认机制：
	- 确认帧ACK_i：若接收方接受到i号帧，且没有检查出差错，需要给发送方返回确认帧ACK_i
- 重传机制
	- 超时重传
- 帧编号
	- 仅需1bit编号


- 异常情况
	- 数据帧丢失
		- 接收方在发送时启动计时器，超时重新传并重置计时器
	- 确认帧丢失
		- 接收方ACK丢失，滑动窗口前移，此时发送方超时重传，接收方收到前一个帧，丢弃重复帧，发送重复帧的ACK
	- 数据帧有差错
		- 收到数据帧，检测出出错，丢弃，不发送ACK；发送方超时重传